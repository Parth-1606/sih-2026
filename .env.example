# Optional — Google Maps mode only. Everything else works without keys.
# Get one at https://console.cloud.google.com (enable Maps JavaScript API).
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# Optional — future live integrations (currently Unavailable in /sources).
# MOSDAC_SSO_USER=
# MOSDAC_SSO_PASS=
# CMEMS_USERNAME=
# CMEMS_PASSWORD=
# SACHET_ENDPOINT=
# GFW_API_TOKEN=
# WDPA_API_TOKEN=
